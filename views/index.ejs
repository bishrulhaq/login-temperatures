<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/header.ejs") %>
</head>

<body id="body">
<%- include("./partials/nav.ejs") %>

<div class="container">
    <div class="row justify-content-md-center mt-5">
        <div class="col-sm-6">
            <div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
                <h1 class="display-4">Hi there!</h1>
                <p>  <%= user.fullname %></p>
                <p class="lead">Explore more with the single signup or if you're already a member then login to
                    continue.</p>
                <p>
                    <a href="#" class="btn btn-primary my-2">myData</a>
                    <a href="#" class="btn btn-secondary my-2">Login</a>
                </p>
            </div>
        </div>
    </div>
</div>

<div v-cloak id="app" class="container">

</div>

<%- include("./partials/footer.ejs") %>
<script>
    let app = new Vue({
        el: '#app',
        data: {},
        mounted: function () {
            axios.get(`${window.location.origin}/get-weather`)
                .then(response => console.log(response))
                .catch(err => {
                    // Manage the state of the application if the request
                    // has failed
                })
        },
        methods: {}
    })
</script>
</body>
</html>